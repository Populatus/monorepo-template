{
  "name": "@saga/monorepo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "start": "pnpm run start --filter '*'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky-install",
    "build": "pnpm run update && esno scripts/build.ts",
    "build:rollup": "rollup -c",
    "clean": "rimraf dist packages/*/dist",
    "update": "pnpm run -C packages/metadata update",
    "csa": "pnpx changeset add",
    "csv": "pnpx changeset version"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vite": "^2.9.5"
  },
  "devDependencies": {
    "@changesets/cli": "^2.22.0",
    "@rollup/plugin-json": "^4.1.0",
    "@types/fs-extra": "^9.0.13",
    "consola": "^2.15.3",
    "esbuild": "^0.14.37",
    "esbuild-register": "^3.3.2",
    "esno": "^0.14.1",
    "fs-extra": "^10.1.0",
    "husky": "^7.0.4",
    "lint-staged": "^12.3.8",
    "rimraf": "^3.0.2",
    "rollup": "^2.70.2",
    "rollup-plugin-esbuild": "^4.9.1"
  }
}